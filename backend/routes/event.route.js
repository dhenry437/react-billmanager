const express = require("express");
const router = express.Router();

// Create new event
router.post("");

// Edit a event
// router.put("/:id");

// Delete a event
// router.delete("/:id");

module.exports = router;
